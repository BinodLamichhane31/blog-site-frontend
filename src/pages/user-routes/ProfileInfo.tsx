import React from 'react';
import Base from "../../component/Base.tsx";

const ProfileInfo = () => {
    return (
        <Base>
            <div>
                Profile Info
            </div>
        </Base>
    );
};

export default ProfileInfo;